model:
  name: bert
  checkpoint: /path/to/checkpoints/bert
training:
  epochs: 10
  batch_size: 8
evaluation:
  metrics:
  - accuracy
  - precision
  - recall
  - f1
data:
  path: ./data
  processed: ${data.path}/processed
  raw: ${data.path}/raw
  labelled: ${data.path}/labelled
labels:
  topics: ${labels.topics}
